# 招聘结构化面试评估系统 · 部署说明（最终版）

## 永久访问链接（GitHub Pages）

```
https://xuyao7695.github.io/hr-interview-tool/
```

> 永久有效，不依赖任何服务器；微信可直接转发。若打开后仍是旧版，请**硬刷新**（手机：退出重进或清理网页缓存；电脑：Ctrl+Shift+R）。
> 曾打开过旧版（仅3个岗位）的浏览器，硬刷新后会**自动升级到 16 个预置岗位**（系统按版本号增量同步，无需手动清空数据）。

## 仓库地址

```
https://github.com/xuyao7695/hr-interview-tool
```

## 已预置岗位（共 16 个，打开即自带）

| 类别 | 岗位 |
|------|------|
| 业务销售（原3） | 风扇灯外贸业务员 · 无人机动力套销售人员 · 无人机动力套跨境电商运营 |
| 生产制造 | CNC师傅 · 数控车操作员 · 无人机电机结构工程师 · 嵌入式硬件工程师 |
| 研发技术 | 技术总监 · 复合材料和机械结构优化工程师 · 飞控软件工程师(中级) · 飞控硬件工程师(中级) · 通讯工程师 |
| 供应链/职能 | 采购员 · 供应链开发工程师 · 适航员 · 财务主管(内控方向) |

每个岗位含 6 个考核维度（权重合计 100%）+ STAR 面试问题，按"选岗位→自动带出结构化问卷与评分权重"逻辑运行。

## 使用流程

1. 浏览器打开永久链接
2. ① 配置：添加面试官、添加候选人（选岗位自动带出对应 STAR 问卷与评分权重）
3. ② 评分：选面试官+候选人 → 整张结构化面试表内联展示 → 逐维度 1-5 分评分 + STAR 记录 → 提交
4. ③ 结果：自动按权重加权汇总（综合分 = Σ维度平均分×权重 / Σ权重），≥4.0 建议录用 / 3.0-3.99 待定 / <3.0 不建议

## 一票否决与破格录用模块（面试自由填写）

评分表单末尾含两个可填写模块，内容由面试官按岗位/候选人自行填写，系统**不预置任何具体项**（如驾照、证件、英语等）：

- **🚫 硬性指标一票否决**：点「＋添加否决项」逐条填写门槛（如：无大电流PCB经验 / 有诚信问题），每项选「达标/不达标」。任一项不达标 → 结果直接判「一票否决·不建议」，无论综合分多高。
- **⭐ 特别优秀破格录用**：填写「破格维度」+「成就证据」。有证据且综合分非否决时 → 结果标记「破格录用」，由 HR 结合多人意见最终确认。

判定优先级：**一票否决 > STAR 加权综合分 > 破格录用**。

## 面试官按岗位分配 / 删除 · 候选人归档（历史不丢）

- **面试官按岗位分配**：在「① 配置 → 管理岗位」每个岗位下方，勾选该岗位对应的面试官；新增面试官默认进入全部岗位，可在具体岗位下取消勾选。不同岗位可分配不同面试官。
- **面试官可删除**：点面试官名旁的 ✕ 即移除；已提交的评分记录**保留**（以"署名归档"形式留在历史中），仅从当前名单与岗位分配中移除。
- **候选人"归档"而非删除**：点候选人「归档」后，其全部评分记录保留、结果页仍可查，仅移出当前名单；归档区提供「恢复」可随时放回。
- **评分页只显示本岗位面试官**：进入「② 评分」选候选人后，面试官下拉仅列出该岗位已分配的面试官，避免串岗。

> 设计原则：任何"删除"都不破坏已产生的面试记录，保证历史可追溯、可审计。

## 多人协作（零服务器，微信传链接）

- HR 点「复制协作链接」→ 微信发面试官
- 面试官打开评分，点「复制我的评分链接」回传
- HR 打开回传链接 → 自动合并 → 结果页汇总
- 链接过长被截断时，改用「导出配置 / 导出评分」JSON 文件经微信发送，再「导入并合并」

## 安全事项（重要）

- 部署用的 GitHub Personal Access Token 已完成使命，**请到 https://github.com/settings/tokens 撤销 `hr-tool` 令牌**
- 撤销不影响已部署页面，仅影响后续 push 更新权限

## 后续更新岗位/维度的两种方式

- **A. 网页内临时加**（最简单）：① 配置 → 点「＋新增面试岗位」，填维度/权重/STAR，存浏览器本地；用「复制协作链接」分享给他人
- **B. 预置进源码**（永久自带）：告诉我调整内容 → 重新给有 `repo` 权限的 Token → 我改源码重新部署 → 你撤销 Token

## 技术要点

- 纯前端单文件（index.html），零后端依赖
- 数据存浏览器 localStorage，不上传任何服务器
- 协作通过 URL hash（Base64）编码实现，零服务器中转

## 本次更新（按使用反馈重构）

- **面试官专属链接**：「复制面试官链接」生成 `?role=interviewer` 链接，面试官打开后**仅见评分页**；可在链接里带 `&iv=<id>` 预选面试官并隐藏选择器。
- **面试官按岗位负责**：新增面试官**不再自动分配全部岗位**，在「管理岗位」下按岗位勾选负责面试官即可。
- **评分页按岗位过滤**：选候选人后面试官下拉仅显示该岗位负责者；切换候选人时下拉随之刷新，支持同一链接连续多场切换。
- **归档 / 硬删**：配置页候选人生「归档」（移出名单、隐藏于面试官下拉，但结果页保留历史、可恢复）与「删除」（彻底移除本人及评分）；结果页每条记录带「🗑 删除」（仅 HR 可见）。
- **格式化报告**：结果页「📄 导出报告」生成含每位面试官评分/评语/最终结果的报告，可打印或存 PDF 转发。

## 云端自动合并（后端部署，可选 · 推荐）

面试官提交即自动上传合并，免去来回传链接。后端为 `server.py`（FastAPI），已本地验证。

### 方式 A：Render 免费部署（推荐）

1. 注册 https://render.com （免费）
2. Dashboard → New → Web Service → 连接 GitHub 仓库 `xuyao7695/hr-interview-tool`
3. 环境选 **Docker**（仓库含 `Dockerfile`），实例类型选 **Free**，端口 `10000`
4. 创建后获得 `https://xxxx.onrender.com`，点开显示 `{"status":"ok"}` 即正常
5. 前端「① 配置 → 云端自动合并」填写该地址 + 自定房间密钥（如 `dk2026`），点「保存云端设置」并「⬆ 上传配置到云端」
6. 「复制面试官链接」发面试官 → 其提交即自动上传；HR 在结果页点「☁ 从云端刷新」汇总

> 注意：免费实例会**休眠**，首次访问可能冷启动 10–30 秒，属正常。
> CORS 已放开（`*`），GitHub Pages 前端可跨域调用。

### 方式 B：自有服务器

```
pip install -r requirements.txt
uvicorn server:app --host 0.0.0.0 --port 10000
```
反向代理到 https 域名后，把地址填进前端「云端地址」即可。

### 数据隔离

按「房间密钥」隔离，文件存于服务端 `cloud_data/<room>.json`；同一密钥的 HR 与面试官共享一份数据。密钥通过链接 `&cloudkey=` 下发，请勿外泄给无关人员。

## 安全事项（重要）

- 部署用的 GitHub Personal Access Token 用后请到 https://github.com/settings/tokens 撤销。
- 撤销不影响已部署页面，仅影响后续 push 更新权限。
- 云端房间密钥等同数据共享凭据，仅发给参与面试的面试官。

